// Step1: Crete a var for temperature and set it equal to 80
// Step 2: Log "The temperature is 80 degrees"
//Step 3: if else statement
// if temp . 80, ouput "time to swim"

var temperature = 80;
var precipitation = 'raining';
var indoors = true

console.log("The temperature is " + temperature + "degrees." );

if (temperature >= 80 && precipitation !== 'raining' && precipitation !== 'snowing') {
	console.log('Time to swim!');
} else if (indoors) {
	console.log("Time to swim!");
}

// Step 4: Create a precipitation variable and set it to false
// Step 5: Only output 'time to swim" if temp is greater than 80 and it's not raining
// Step 6: Set the precipitation variaable to 'raining' or 'snowing' and only output 'time to swim' if there is no precipitation
// Step 7: Create an 'indoors' variable and set it to true
// Step 8: If indoors, then output 'time to swim' regardless of temp and precipitation
// create a variable called indoors
//if indoors, log "time to swim" regardless of precipiation and temperature





